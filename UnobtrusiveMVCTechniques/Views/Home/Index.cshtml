@model dynamic

@{
    ViewBag.Title = "Unobtrusive MVC Techniques";
}

<h1>Unobtrusive MVC Techniques</h1>

<h2>Unobtrusive Validation Example</h2>
<p>@Html.ActionLink("Unobtrusive Validation Example", "ValidationExample")</p>

<h2>Custom Validation Approaches Comparison</h2>
<p>For the following they will validate that username is required and that the username isn't taken (the only "taken" username in this case is "dummy")</p>
<ol>
    <li>@Html.ActionLink("Custom validation code in controller", "CustomCodeInController", "CustomValidation")</li>
    <li>@Html.ActionLink("Modularised custom validation code called from controller", "ModularisedCustomCodeCalledFromController", "CustomValidation")</li>
    <li>@Html.ActionLink("Get default model binder to call the validation code before the controller action", "ValidationAlreadyCalled", "CustomValidation")</li>
    <li>@Html.ActionLink("Introduce a validation library", "FluentValidation", "CustomValidation")</li>
    <li>@Html.ActionLink("Use ModelValidatorProvider", "ModelValidatorProvider", "CustomValidation")</li>
</ol>
